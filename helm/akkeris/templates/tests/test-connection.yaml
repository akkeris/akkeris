apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akkeris.fullname" . }}-test-connection"
  labels:
{{ include "akkeris.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "akkeris.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
