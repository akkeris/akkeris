apiVersion: v1
kind: Service
metadata:
{{- if .Values.regionApi.service.annotations }}
  annotations:
{{ toYaml .Values.regionApi.service.annotations | indent 4 }}
{{- end }}
  labels:
    {{- include "akkeris.regionApi.labels" . | nindent 4 }}
{{- if .Values.regionApi.service.labels }}
{{ toYaml .Values.regionApi.service.labels | indent 4 }}
{{- end }}
  name: {{ template "akkeris.regionApi.fullname" . }}

spec:
{{- if .Values.regionApi.service.clusterIP }}
  clusterIP: {{ .Values.regionApi.service.clusterIP }}
{{- end }}
{{- if .Values.regionApi.service.externalIPs }}
  externalIPs:
{{ toYaml .Values.regionApi.service.externalIPs | indent 4 }}
{{- end }}
  type: {{ .Values.regionApi.service.type }}
  ports:
    - port: {{ .Values.regionApi.service.servicePort }}
      targetPort: 3600
      protocol: TCP
      name: http
    {{- if .Values.regionApi.service.nodePort }}
      nodePort: {{ .Values.regionApi.service.nodePort }}
    {{- end }}
  selector:
    {{- include "akkeris.regionApi.matchLabels" . | nindent 4 }}
  type: "{{ .Values.regionApi.service.type }}"
